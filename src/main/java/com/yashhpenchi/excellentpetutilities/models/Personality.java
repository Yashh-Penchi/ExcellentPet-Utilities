package com.yashhpenchi.excellentpetutilities.models;

// rolled once on first tame, cosmetic-ish, small bonus only - actual bonus values
// live in abilities.yml once the ability system exists, not hardcoded here
public enum Personality {
    AGGRESSIVE,
    PROTECTIVE,
    LOYAL,
    SWIFT,
    LUCKY
}
